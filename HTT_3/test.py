#
# Quiz
#Python code which prompts the user to enter an integer from the user, then reads it and
# prints out its value multiplied by a random float in the range from [0.0, 1.0).
#
import random
a = input("Enter an integer")

print(int(a) * random.ra)